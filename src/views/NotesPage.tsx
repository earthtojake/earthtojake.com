export function NotesPage() {
  return (
    <main className="px-4 py-8">
      <section className="mx-auto max-w-3xl">
        <h1 className="m-0 text-3xl font-semibold leading-tight">Simple Page (No Slide Reveal)</h1>
        <p>
          This route demonstrates that your site can contain regular pages with native
          scrolling and no reveal logic.
        </p>
        <p className="mt-3.5 text-base leading-6">
          Keep this as your base template for blog posts, project write-ups, or anything
          that should not use the landing page interaction.
        </p>
      </section>
    </main>
  );
}
